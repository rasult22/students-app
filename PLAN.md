# План реализации: Мини-игра "Квест героя" (game-quest)

## Обзор

RPG-игра с пиксельным стилем, где герой сражается с монстрами, отвечая на вопросы по теме. Каждый правильный ответ — атака по врагу, неправильный — враг атакует героя.

## Игровая механика

- **Цель:** Победить 5 врагов до потери 3 жизней
- **Система боя:**
  - Правильный ответ = атака героя → урон врагу
  - Неправильный ответ = враг атакует → -1 жизнь героя
- **Враги:** 5 разных монстров (слайм, скелет, гоблин, орк, босс-дракон)
- **Визуал:** Пиксельный/ретро стиль с анимациями атак

## Фазы игры

1. `idle` - Герой и враг стоят друг напротив друга, ожидание ответа
2. `attacking` - Анимация атаки героя (при правильном ответе)
3. `damaged` - Анимация получения урона героем (при неправильном)
4. `enemy_defeated` - Враг побеждён, переход к следующему
5. `result` - Показ результата раунда

## Структура файлов

```
src/components/MiniGames/games/QuestGame/
├── QuestGame.tsx           # Основной компонент игры
├── QuestGame.module.css    # Стили с пиксельной эстетикой
└── index.ts                # Экспорт
```

## Компоненты UI

### Арена боя (верхняя часть)
- Фон: пиксельный данжен/пещера с градиентами
- Слева: Герой (пиксельный рыцарь/воин) с анимациями
- Справа: Текущий враг (монстр) с HP-баром
- Эффекты: вспышки при атаках, тряска при уроне

### Статус бар
- Жизни героя: 3 сердечка (как в FootballGame)
- Счётчик побеждённых врагов: X / 5
- Номер текущего врага и его имя

### Область вопросов (нижняя часть)
- Вопрос от врага (в стиле диалога RPG)
- 4 варианта ответа как кнопки способностей
- Кнопка "Атаковать!" вместо "Ответить"

## Враги (5 уровней)

| # | Имя | Emoji | Сложность |
|---|-----|-------|-----------|
| 1 | Слайм | 🟢 | Легко |
| 2 | Скелет | 💀 | Легко |
| 3 | Гоблин | 👺 | Средне |
| 4 | Орк | 👹 | Средне |
| 5 | Дракон | 🐉 | Сложно |

## Анимации (Framer Motion)

1. **idle**: Плавное покачивание персонажей
2. **hero_attack**: Герой движется вправо → удар → возврат
3. **enemy_attack**: Враг движется влево → удар → возврат + тряска экрана
4. **enemy_defeat**: Враг мигает → исчезает → появляется новый
5. **damage_flash**: Красная вспышка при получении урона

## Состояние компонента

```typescript
interface QuestGameState {
  currentEnemyIndex: number;      // 0-4, какой враг сейчас
  currentQuestionIndex: number;   // Индекс текущего вопроса
  score: number;                  // Побеждённых врагов (0-5)
  lives: number;                  // Жизни героя (0-3)
  phase: 'idle' | 'attacking' | 'damaged' | 'enemy_defeated' | 'result';
  selectedAnswer: number | null;
  lastResult: 'hit' | 'miss' | null;
}
```

## Шаги реализации

### 1. Создать структуру файлов
- Создать папку `QuestGame`
- Создать `QuestGame.tsx`, `QuestGame.module.css`, `index.ts`

### 2. Реализовать базовый компонент
- Props: `questions`, `onComplete`, `onExit`
- State: управление фазами боя
- Логика переключения врагов

### 3. Создать CSS стили
- Пиксельный фон арены (CSS градиенты)
- Стили для персонажей (emoji + эффекты)
- HP бар для врага
- Сетка кнопок ответов в RPG стиле
- Адаптивная вёрстка

### 4. Добавить анимации
- Анимации атаки героя
- Анимации атаки врага + тряска
- Переход между врагами
- Эффекты урона и победы

### 5. Интегрировать в MiniGames.tsx
- Добавить импорт QuestGame
- Добавить условие `selectedGame === 'game-quest'`
- Передать props

### 6. Тестирование
- Проверить игровой цикл
- Проверить все переходы между врагами
- Проверить условия победы/поражения
- Проверить мобильную адаптацию

## Визуальный стиль

- Цветовая схема: использовать design-tokens.css
- Фон арены: тёмные пурпурные/синие тона (данжен)
- Акценты: зелёный для хитов, красный для урона
- Шрифт: сохранить Outfit для заголовков
- Emoji для персонажей (простота + кроссплатформенность)

## Ожидаемый результат

Полностью играбельная RPG мини-игра с:
- 5 уникальными врагами
- Плавными анимациями боя
- Системой жизней и очков
- Интеграцией с существующей системой вопросов
