# План реализации игры "Режиссёр" (movie-scenes)

## Концепция игры

**Тема:** Создание киносцены
**Механика:** Пользователь отвечает на вопросы, каждый правильный ответ добавляет элемент сцены на "кадр фильма". 5 элементов = победа, 3 ошибки = поражение.

## Визуальная концепция

### Элементы сцены (5 этапов прогресса):
1. **Фон/Локация** - пустой кадр → появляется фон сцены (эмодзи: 🏞️/🏙️/🌃)
2. **Персонаж** - добавляется главный герой (эмодзи: 👤/🧑‍🎤/🦸)
3. **Действие** - добавляется элемент действия (эмодзи: 💥/❤️/🔍)
4. **Диалог** - появляется "облачко" диалога (эмодзи: 💬)
5. **Концовка** - финальный кат! (эмодзи: 🎬)

### Визуальные эффекты:
- Киношная рамка с "плёнкой" по бокам (перфорация)
- Хлопушка режиссёра анимируется при старте вопроса
- При правильном ответе - элемент "влетает" в кадр с эффектом
- При неправильном - помеха (статический шум/искажение)
- Прогресс-бар в виде плёнки внизу

### Цветовая схема:
- Основной: тёмный кинозал (#1a1a2e → #16213e)
- Акцент: красный/золотой (кинематографическая тема)
- Свечение прожекторов

## Структура файлов

```
src/components/MiniGames/games/MovieGame/
├── MovieGame.tsx          # Основной компонент
├── MovieGame.module.css   # Стили
└── index.ts               # Экспорт
```

## Компонент MovieGame.tsx

### Props (аналогично FootballGame/QuestGame):
```typescript
interface MovieGameProps {
  questions: MiniGameQuestion[];
  onComplete: (result: 'won' | 'lost', stats: { score: number; errors: number }) => void;
  onExit: () => void;
}
```

### State:
```typescript
type GamePhase = 'directing' | 'filming' | 'result';
type FilmResult = 'success' | 'blooper' | null;

const [currentIndex, setCurrentIndex] = useState(0);
const [score, setScore] = useState(0);
const [lives, setLives] = useState(3);
const [selectedAnswer, setSelectedAnswer] = useState<number | null>(null);
const [phase, setPhase] = useState<GamePhase>('directing');
const [filmResult, setFilmResult] = useState<FilmResult>(null);
```

### Элементы сцены:
```typescript
const SCENE_ELEMENTS = [
  { id: 1, name: 'Локация', emoji: '🏙️', unlockEmoji: '🎞️' },
  { id: 2, name: 'Персонаж', emoji: '🦸', unlockEmoji: '⭐' },
  { id: 3, name: 'Действие', emoji: '💥', unlockEmoji: '🎭' },
  { id: 4, name: 'Диалог', emoji: '💬', unlockEmoji: '🎤' },
  { id: 5, name: 'Концовка', emoji: '🎬', unlockEmoji: '🏆' },
];
```

## CSS Стили (MovieGame.module.css)

### Основные секции:
1. `.container` - основной контейнер
2. `.header` - заголовок с жизнями и прогрессом (аналог других игр)
3. `.filmFrame` - кинематографическая рамка
4. `.sceneArea` - область "кадра" где появляются элементы
5. `.filmStrip` - боковые полосы плёнки с перфорацией
6. `.clapperboard` - хлопушка режиссёра (анимация)
7. `.sceneElement` - анимированный элемент сцены
8. `.resultOverlay` - оверлей с результатом (Снято!/Дубль!)
9. `.questionArea` - область вопроса (переиспользуем паттерн)
10. `.progressTrack` - прогресс в виде плёнки

### Анимации:
- `clapAnimation` - хлопушка закрывается при начале
- `elementEnter` - элемент влетает в кадр
- `staticNoise` - статический шум при ошибке
- `spotlightGlow` - свечение прожекторов

## Интеграция в MiniGames.tsx

Добавить:
1. Импорт `MovieGame` из `./games/MovieGame`
2. Условный рендер для `movie-scenes` в фазе `playing`:

```tsx
{phase === 'playing' && selectedGame === 'movie-scenes' && questions.length > 0 && (
  <motion.div key="playing-movie" ...>
    <MovieGame
      questions={questions}
      onComplete={(result, stats) => {
        setFinalStats(stats);
        setGameResult(result);
        setPhase('results');
      }}
      onExit={handlePlayAgain}
    />
  </motion.div>
)}
```

## Шаги реализации

1. Создать директорию `src/components/MiniGames/games/MovieGame/`
2. Создать `MovieGame.tsx` с логикой игры
3. Создать `MovieGame.module.css` со стилями
4. Создать `index.ts` для экспорта
5. Интегрировать в `MiniGames.tsx`
6. Проверить работоспособность
